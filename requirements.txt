numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
xarray>=0.19.0
matplotlib>=3.4.0
h5py>=3.0.0
yt>=4.0.0
pyyaml>=5.4.0
astropy>=5.0.0
pynucastro>=2.8.0

# Note: The following packages are custom/local dependencies that need to be installed separately:
# - flashbang (custom package for tracer data)
# - nucleosynth (custom package for nucleosynthesis calculations)
# - progs (local package in progs/ directory)

